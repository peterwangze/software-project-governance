# 0.85.0 批次候选池依赖分析（Analyst 半面）

- **Task**: 0.85.0-PLAN/A（规划工作标签——非 plan-tracker 任务，不触发 change-triage）
- **授权**: DEC-215②（0.85.0 批次规划启动；候选序 ①~⑧）
- **分析日期**: 2026-09-19（工作树基线 = master `d62066b`；文中行号均为当日 Read/Grep 实测，后续 commit 落地后行号可能漂移——blob/锚点语义优先）
- **修订记录**: **R1**（REL-081 双 Reviewer R0 均 NEEDS_CHANGE 返工——逐 finding 响应：Design 报告 D-F-2/4/5/6/7/8 承接 + Release 报告边缘 ④ 措辞澄清；批次归属按双审裁决**方案 A** 同步。本文件为 Analyst 半面修订版，Release 半面 `docs/release/version-plan-0.85.0.md` 修订并行进行）
- **方法**: 治理记录锚点（decision-log / risk-log / evidence-log / plan-tracker / docs/reviews/）逐项回溯 + file targets 逐一 Grep/Read 实证。本分析不做技术选型、不写代码、不改 `.governance/`；本文件是本次任务唯一写入面。
- **角色**: Analyst（agents/analyst.md）｜**任务规范**: requirement-clarification SKILL（本任务用作候选逐项边界确认与验收信号框架——每项含 IN/OUT（非目标）、假设显式化、可验证验收信号）

---

## 摘要表（8 项）

| # | 候选 | 来源锚点 | file targets（已实证） | 依赖 | 优先级建议 | 关键假设 |
|---|------|---------|------------------------|------|-----------|---------|
| 1 | standard/strict 入口模板瘦身 + ADVISORY→hard 翻转前置 | DEC-210；DEC-211①②③；RISK-057/058；REVIEW-FEAT-039-CODE-R0/R1 | `commands/governance-init.md`（Step 7 模板块）；`infra/checks/injection_budget.py` L166-179；`infra/sync_entry_projection.py` L118；`infra/tests/test_verify_workflow.py` Feat039InjectionBudgetTests | →FEAT-039 P3-3（**批 2.3 同 commit**——方案 A）；与候选 8 同落**批 2 串行** | **P1** | 瘦身可达标（超限量 3,953/4,154 tok）可行性未验证（批 2 前置 2.0 量测=开工否决点） |
| 2 | C-01 豁免行措辞分流 + FIX-341 谓词分歧 13 行假红 | review-FIX-355-CODE-R0（C-01 源判 P1 / C-02 P2，V-7~V-10 实证）；DEC-214 | `infra/checks/review_domain.py` L2180-2190、L2559-2583+L2623；`infra/tests/test_review_closure_legacy.py` | 独立；同报告 C-03/C-05 未入池（边缘披露） | **P1**（C-01 源判 P1 承接） | 分流不改判定（review 已断言）；并集方向安全（review 建议） |
| 3 | FIX-356 F-1 CALIBRATION 披露行 | review-FIX-356-CODE-R0（F-1 P2）；plan-tracker L95 | `infra/governance_cost.py` L736（CALIBRATION）；`infra/tests/test_governance_cost.py`（回归） | 独立 | **P2**（顺带项，改动极小） | 增键不破坏消费面（grep 证实仅模块内消费）；resident 注入面风险已关闭（D-F-8 实测） |
| 4 | 测试静态版本钉机检面 | DEC-213③/⑦ | 新机检落点候选面：`infra/checks/version.py`（扩展）或新 leaf + `infra/registry.py` + `infra/verify_workflow.py` + `infra/tests/test_registry.py` 冻结面 + `contract_matrix/snapshots.json`——**待设计确认** | 与候选 5 同族可同批设计；弱 | **P2** | 静态扫描误报率未知（豁免清单先例 FIX-354） |
| 5 | governance-status.md fixture 同族缺口 | EVD-1084（同族发现原文）；FIX-354/R1 F-05 先例；DEC-213⑦ 同族 | `project/e2e-test-project/commands/governance-status.md`（fixture）；`skills/software-project-governance/core/version-projections.json`；`core/manifest.json`（若 promote）；e2e 消费面 `infra/tests/test_verify_workflow.py` L3031/L3068 | 与候选 4 同族；独立可裁 | **P2** | 分歧量未实测（pre-FIX-270 陈旧、无版本标记） |
| 6 | P3 族（FEAT-038 P3-1/2/5、FEAT-039 F-1/2/5+P3-3/4/7、FEAT-040 P3-1~4+P3-R1-1~4、PLAN_TRACKER_UPGRADED 死夹具、FIRST_RUN_DEMO 19→20） | REVIEW-FEAT-038-CODE-R0；REVIEW-FEAT-039-CODE-R0/R1 + DEC-211③④；REVIEW-FEAT-040-CODE-R0/R1 + DEC-212⑤；EVD-1083（死夹具）；DEC-215② | 分见 §7 逐子项表（8 个文件面 + 2 个治理记录面） | FEAT-039 F-1/2/5/P3-3/P3-4 拆出落**批 2**（方案 A）；其余子项留批 1 独立 | **P3**（族内 P3-7/P3-2 属治理记录快速通道） | FIRST_RUN_DEMO 19→20 意图推断=补第 20 字段（待设计确认） |
| 7 | RISK-056 回放族 | RISK-056（risk-log L52）；REVIEW-REL-080-RELEASE-R1 V-11 实测 | `infra/tests/test_hooks.py` L202；`infra/tests/test_pre_commit_review_evidence.py` L122；`infra/tests/test_verify_workflow.py` L1592/L10647 | 独立 | **P3**（RISK-056 中等严重度支持升 P2；DEC-215② 授权序列第 7） | 30F 全为环境/活体耦合归因未逐项复验 |
| 8 | entry-skill 独立预算 | DEC-210(ii)；DEC-211②；REVIEW-FEAT-039-CODE-R0/R1 | `infra/checks/injection_budget.py`（INJECTION_BUDGET_SURFACES L140-147 + BUDGET_TIER_POLICY L171-174）；`infra/tests/test_verify_workflow.py`；`infra/TOOLS.md` TOOL-055 | 与候选 1 同文件强耦合——同落**批 2 串行**（方案 A） | **P2** | 「独立预算」=skill tier 立独立数值+gate 姿态（待设计确认） |

---

## 1. standard/strict 入口模板瘦身 + check-injection-budget ADVISORY→hard 翻转前置

### ① 来源锚点（已核实）
- **DEC-210**（decision-log L151）：注入预算门禁出货姿态=advisory；「翻 hard 时点：0.85.0 完成模板瘦身后」；回弹风险登记 RISK-057。
- **DEC-211②**（decision-log L152）：口径刷新为 canonical——standard/strict resident **9,953/10,154**（超 **3,953/4,154** tok）；lightweight 4,288/6,000 PASS；全域 55,214B；advisory 姿态维持。
- **DEC-211③**：FEAT-039 P3-3（hard 触发路径无测试）→ 0.85.0 翻 hard 时补——本候选的强制伴随项。
- **RISK-057**（risk-log L53）：advisory 态回弹风险；缓解③=「0.85.0 候选池已含 standard/strict 入口模板瘦身（瘦身后翻 hard）」。
- **RISK-058**（risk-log L54）：resident +669 tok（4,288→4,957，余量 17.4%）；缓解③④=「0.85.0 模板瘦身任务把灰度开关描述纳入压缩目标；翻 hard 时点=瘦身后（DEC-210）」；DEC-212⑦ 同口径。
- **FEAT-039 交付时点超限事实**（plan-tracker L87）：FEAT-039 R1 复审 APPROVED_WITH_NOTES/0，standard/strict ADVISORY 出货（DEC-210 姿态）。

### ② file targets（Grep/Read 实证）
| 文件 | 角色 | 证据 |
|------|------|------|
| `commands/governance-init.md` | 瘦身主对象——Step 7 的 standard/strict 注入模板 canonical 块 | `infra/checks/injection_budget.py` L78-83 `ENTRY_TEMPLATE_MARKERS`（standard/strict 标记锚）+ L109-118 `entry-template` surface（path=`commands/governance-init.md`，profile-selected template block） |
| `skills/software-project-governance/infra/checks/injection_budget.py` L166-179 | 翻硬面——`BUDGET_TIER_POLICY["resident"]["gate"]: "advisory"`→`"hard"`（DEC-210：「一行数据改动被测试钉住」） | Read L161-179 实测当前值 advisory |
| `skills/software-project-governance/infra/tests/test_verify_workflow.py`（Feat039InjectionBudgetTests，L20046-20600 区段） | 翻硬伴随测试翻转——L20367 断言 `BUDGET_TIER_POLICY["resident"]["gate"] == "advisory"`、L20368/L20377 等 ADVISORY 断言需同步改 hard；另补 FEAT-039 P3-3 的 `injection_budget_tier_gate("unknown") == "hard"` 断言 | Grep 实测 L20367-20368/L20377 |
| `skills/software-project-governance/infra/sync_entry_projection.py` L118 `extract_canonical_templates` | 模板 extent 权威解析（DEC-211① 口径权威）——瘦身后块边界由其渲染，须与预算计量一致 | Grep 实测存在；DEC-211① 原文 |
| 同步义务面 | `commands/governance-init.md` 是 `canonical-bootstrap-version` transformed_text 投影 target（version-projections.json L124-131，count=4）+ `fixture-command-governance-init` byte_copy source（L132-137）——瘦身后 MUST `release-projection --write` 重生成 | version-projections.json Read 实测 |

**待设计确认面**：`injection_budget.py` L352 `canonical_entry_templates` 与 `sync_entry_projection.py` L118 `extract_canonical_templates` 为两套块边界实现（REVIEW-FEAT-039-CODE-R1 F-2：「同模块内仍存在一套不被任何解析路径使用的私有边界实现」）——瘦身时两处口径是否统一属设计决策，本分析不裁决。

### ③ 依赖关系
- **对既有任务/风险**：DEC-210 明文把「翻 hard」设定为本候选完成后的动作；RISK-057/058 的缓解承诺（③④）均指向本候选——本候选是两条打开风险的关闭前置。
- **项间依赖**：含候选 6 内 FEAT-039 P3-3（hard 触发路径测试，**并入批 2.3 翻 hard 同 commit**——REL-081 双审裁决方案 A，兑现 DEC-211③「翻 hard 时补」时点绑定）；与候选 8 同文件（`injection_budget.py` 的 `BUDGET_TIER_POLICY`/`INJECTION_BUDGET_SURFACES`）——**候选 1/8 与 FEAT-039 F-1/F-2/F-5/P3-4 全部落批 2 串行**（方案 A；文件锁协调 MUST，候选 1 先行）。

### ④ 优先级建议：**P1**
理由：DEC-210 设定的翻 hard 时点=瘦身后（授权链裁决，非建议）；RISK-057/058 两条打开风险的缓解动作落在本候选；0.85.0 候选池首位（DEC-215② 授权序 ①）。

### ⑤ 非目标
- 不放宽 6,000 预算值本身（DEC-210 论证不依赖数字档位；放宽=另一裁决）。
- 不动 lightweight 档（4,288/6,000 PASS，余量 28.5%）。
- 不改 token 计价口径（CJK 1/char、ASCII ceil(chars/4)——TOOLS.md TOOL-055 已文档化）。
- 不在本候选内处理 entry-skill 面（候选 8 分立，防范围漂移——但注意同文件协调）。

### ⑥ 验收信号
1. `check-injection-budget --profile standard` 与 `--profile strict` resident 面 ≤6,000 tok（`--format json` 的 `verdict=PASS`）。
2. `BUDGET_TIER_POLICY["resident"]["gate"] == "hard"` 后，合成超限面触发 FAIL（exit 1）——由测试钉住（含 FEAT-039 P3-3 的 `injection_budget_tier_gate("unknown")=="hard"` 断言）。
3. `check-projection-sync` PASS（governance-init.md 双投影合同重生成后一致）；fixture byte_copy 逐字节同。
4. `sha256_16` 面级指纹刷新且 check-governance 分项表无 ADVISORY 残留行。
5. RISK-057/058 具备关闭条件证据（DEC-210 时点承诺兑现）——**措辞限定（Release R0 边缘 ④）：本项验收=「具备关闭条件的证据」≠ 关闭承诺；RISK 关闭动作仍由 Coordinator 按风险行走链执行，不随版本发布自动发生**。

### ⑦ 假设显式化
- **A1-1（未验证）**：standard/strict 模板存在 ≥3,953/4,154 tok 的可压缩冗余，使瘦身后 ≤6,000 可达。验证计划：开工前先产出模板块构成量测（分节 token 分布）——已编排为批 2 序列前置「**2.0 量测**」步骤（REL-081 双审裁决 F-1 修复，结果落 EVD）；量测否决/瘦身后不达标/翻 hard 前不可达时，统一按 Release 文档修订后的**单一回退决策树**三节点处置（本分析不再独立枚举回退选项——防三处不一致复发，见 §10.6）。
- **A1-2（未验证）**：瘦身后 `extract_canonical_templates`/`canonical_entry_templates` 双实现不产生口径分歧。验证计划：设计期对照两函数对瘦身后文本的切块结果。
- **A1-3（已裁决 IN——REL-081 双审裁决 D-F-7，原「待设计确认」标注删除）**：persona/agent-instructions 面的开关文本压缩**纳入**本候选（批 2.1）——授权链两处明文：DEC-212⑦ 原文「0.85.0 模板瘦身**应含**开关文本压缩」+ RISK-058 缓解③；Release 批 2.1 已把「含灰度开关协议文本压缩」写入序列。压缩面 = `adapters/dsh/AGENTS.md.template`（agent-instructions +208 开关文本）+ `agent-presets/governance/agent.cordis.yml.template`（persona +182，RISK-058 缓解②③）。

---

## 2. C-01 豁免行因果断言措辞按来源分流 + FIX-341 谓词分歧 13 行潜在假红

### ① 来源锚点（已核实）
- **review-FIX-355-CODE-R0**（docs/reviews/review-FIX-355-CODE-R0.md）：
  - **C-01（P1，源判）**L129-134：L-A 豁免理由对归档命中实例不成立（审计面失真）——现网命中 2 例（REL-078=DEC-203「轮号纪元偏移」、FIX-246=DEC-199「真实记录缺口」）的理由文本 `legacy leading round gap … audit-148 §3.1 ARCH-001/DEV-002 pattern` 与记录冲突；L-A 是唯一无边界的历史豁免分支。
  - **C-02（P2）**L136-141：归档谓词覆盖缺口——真实语料 372 个归档闭环 ID 中 **13 个（3.5%）漏判**（`完成 +`/`完成——` 散文格不被 `_status_is_completed_cell` 兜底正则覆盖）；V-8 三条反例逐条复现 FAIL（**潜在假红**）；13 ID 清单：FIX-165 / REL-048 / AUDIT-131 / AUDIT-134 / AUDIT-137 / AUDIT-138 / FIX-152 / FIX-214 / FIX-263 / REQ-101 / REQ-104 / REQ-105 / REQ-106。
  - **V-7**：FIX-355 谓词 vs FIX-341 谓词双向分歧 65 行（52 over-include / 13 under-include）——即任务池所称「FIX-341 谓词分歧 13 行」（under-include 方向=潜在假红）。
- **plan-tracker L94**：FIX-355 行尾「C-01（源判 P1）/P2 残留登记 0.85.0」。
- **DEC-214**（decision-log L155）：层级变更（FAIL→WARN）已授权；review C-01 裁决原文「DEC-214 授权的是层级；它未授权理由文本」。
- **REVIEW-REL-080-RELEASE-R3 L-8 / R4 复核**：C-01 标级 P1 与源报告一致性已确认。

### ② file targets（Read 实证，HEAD `d62066b` 时点行号）
| 文件 | 位置 | 内容 |
|------|------|------|
| `skills/software-project-governance/infra/checks/review_domain.py` L2180-2190 | C-01 改点 | 触发门（L2180-2181 `_missing_rounds_are_leading(...) and task_id in closed`）+ 理由模板（L2185-2188 硬编码 `legacy leading round gap … ARCH-001/DEV-002 pattern` 文案）——分流=按归档命中/活体命中给不同 reason |
| 同文件 L2555-2583 + L2623 | C-02 改点 | `_ARCHIVE_TASK_SECTION_HEADING`/`_ARCHIVE_TASK_ID_RE`（L2555-2556）+ `_archived_completed_task_ids()` docstring（L2559-2583）+ 谓词调用（L2623 `_status_is_completed_cell(cells[2])`）——修法二选一：cell 归一化（去 `**`/尾注、接受 `完成` 后接 `+/—`）或与 FIX-341 谓词取并集 |
| `skills/software-project-governance/infra/task_priority.py` L617-628 | C-02 并集选项参照 | FIX-341 谓词先例（`read_archive_index_completed_ids`/`_ID_CELL_RE`/`_strip_markdown`；C-06 已指出常量重复分叉面） |
| `skills/software-project-governance/infra/tests/test_review_closure_legacy.py` | 测试面 | C-01 ±1 用例 + C-02 ±3 条锁形用例（review 建议量）；现 44 用例基线（V-1/V-2） |

**范围边界（防漂移）**：同报告 C-03（UnicodeDecodeError 契约）/C-04（DEC-214 补记——治理记录面）/C-05（3 分支用例）**未入 DEC-215② 候选池**——本候选仅覆盖 C-01+C-02；是否顺带见 §10 边缘问题。

### ③ 依赖关系
- 独立于其余候选；与其他候选无文件交集（review_domain.py 仅本候选触及）。
- 对既有记录的依赖：DEC-214 措辞口径（分流文案的中性表述需引用 DEC-214/DEC-203）；C-04 建议的 DEC-214 补记若由 Coordinator 同窗处理，文案应与本候选分流措辞一致（弱协调项）。

### ④ 优先级建议：**P1**
理由：C-01 为源判 P1（输出真实性缺陷——豁免行给出不可复查的因果断言，与 DEC-199「如实保留」口径冲突），plan-tracker L94 已按 P1 登记 0.85.0；C-02 虽 P2 但与 C-01 同文件同报告，合并为一个任务可一次走完审查链。与 DEC-215② 授权序 ② 一致。

### ⑤ 非目标
- 不改变 FAIL/WARN 判定逻辑（review C-01 明示「不改变 FAIL/WARN 判定，不影响 V2=0 达成」）。
- 不重开 DEC-199/DEC-203/DEC-214 的层级裁决（已闭环）。
- 不处理 52 个 over-included 方向（当前无害——V-10：真实语料中含「已完成」且含开放标记的行 0 例；潜在 fail-open 已由 review 记录在案）。
- 不做 C-03/C-05（未入池——除非用户裁决扩面）。

### ⑥ 验收信号
1. 构造归档命中场景（temp `.governance` + 真 `check_review_closure()`，既有测试方法学）：WARN 行 reason 为中性归档文案，不含 `audit-148 §3.1 ARCH-001/DEV-002 pattern` 断言；活体命中场景保留原文案。
2. V2 违规计数维持 0（check-governance --level strict 无 V2 violation——现基线 29 WARN 不变）。
3. C-02：13 个已知 ID（review 清单）经真实语料比对全部判终态（under-include 归零）；3 条 V-8 反例形态不再触发 FAIL。
4. `pytest test_review_closure_legacy.py` 全绿（44+新增）。
5. 复审走链：本项改动触及 FIX-355 复审基线 blob `e681bfa`（review binding note 5）——MUST Developer→Code Reviewer 走链（DEC-214④ 先例），复审前基线失效条款照抄引用。

### ⑦ 假设显式化
- **A2-1（假设，低风险）**：分流实现为「按来源参数化 reason 文案」，不触碰判定门——review 建议原文如此，但最终实现形态待设计。
- **A2-2（未验证）**：行号漂移——本表行号为 `d62066b` 时点；若候选 2 之前有 commit 触及 review_domain.py，行号须重锚（blob 语义）。
- **A2-3（未验证）**：C-02 若取「并集」方案，13 ID 的并集引入是否影响 38 个重复 ID 的现有边界（review V-7 记录重复 ID 38 个、同 ID 冲突 0——当前无冲突实证，并集后仍需回归验证）。验证计划：实现期以 V-7 同方法复跑谓词比对。

---

## 3. FIX-356 F-1：sessions[].cwd 双源语义 CALIBRATION 披露行

### ① 来源锚点（已核实）
- **review-FIX-356-CODE-R0**（docs/reviews/review-FIX-356-CODE-R0.md）F-1（P2，非阻塞建议）L63：`sessions[].cwd` 语义现为双源——事件 cwd（旧形状，L680-685 守卫）或目录推导 token（新回退，L625-651）；该 provenance 未入 `CALIBRATION` 披露面（L736）；建议原文：`CALIBRATION` 增一行 `"cwd": "event cwd when present; else directory-derived workspace token (RISK-050/FIX-356)"`。
- **plan-tracker L95**：FIX-356 行尾「F-1（P2 CALIBRATION 披露）残留登记 0.85.0」。
- **JSON 消费面事实**（review 实测）：infra 外无消费点（grep 证实）；RISK-052 跨时点 diff 口径不受阻（无 `--workspace` 时 totals 不受 cwd 影响）。

### ② file targets（Grep 实证）
| 文件 | 位置 | 内容 |
|------|------|------|
| `skills/software-project-governance/infra/governance_cost.py` L736 | 改点 | `CALIBRATION = {`（Read 实测）；增 `cwd` 键一行披露（review 建议原文措辞） |
| 同文件 L727 | 消费点（只读确认） | `"calibration": CALIBRATION` 内嵌 report dict——增键随 report 输出，无独立接线 |
| `skills/software-project-governance/infra/tests/test_governance_cost.py` | 回归面 | 既有 58 用例回归（review 六.1 基线）；是否新增 CALIBRATION 键断言待设计确认 |

**爆炸半径核验（本分析实测）**：全 infra grep `CALIBRATION` 仅 3 命中（L71 注释/L727 内嵌/L736 定义）——无测试钉住键集、无 contract_matrix 条目、无外部消费者。改动面 = 单文件单行 + 可选测试断言。

**resident 注入面风险关闭（REL-081 双审裁决 D-F-8——Design R0 F-8 实测；候选 3 triage 时可直接引用）**：`INJECTION_BUDGET_SURFACES` 全部 6 面（`injection_budget.py` L101/111/121/132/141/150——persona、governance-init ×2、AGENTS.md.template、SKILL.md、governance.md）**不含 `governance_cost.py`**——CALIBRATION 披露行不可能落 resident 注入面，「③ 落 resident 面吃余量」类关切（Release 开放点 A1 同源）凭此实测关闭。

### ③ 依赖关系
- 完全独立——无项间依赖、无既有任务依赖（FIX-356 已闭环，本项为其唯一残留）。

### ④ 优先级建议：**P2**
理由：非阻塞披露完整性项（review P2 定级）；改动极小（1 行 + 可选断言），适合作为同批「顺带项」或与其他 governance_cost 触碰任务合批；不建议 P1/P0（无阻断面、无消费者受阻）。

### ⑤ 非目标
- 不改 cwd 推导逻辑本身（FIX-356 已闭环复审）。
- 不动 REPORT_SCHEMA 其他键（review 五：report dict 键集合不变是 FIX-356 non_goals，本项同样只增 CALIBRATION 内部键）。
- 不处理 F-2/F-3（dsh 编码有损边界、`----` 用例——review P3，未入池）。

### ⑥ 验收信号
1. `governance-cost-report --format json` 输出 `calibration.cwd` 键，值披露双源语义（含 RISK-050/FIX-356 指针）。
2. 既有 58 用例零回归（`pytest test_governance_cost.py` exit 0）。
3. TOOLS.md 若有 CALIBRATION 键枚举面（实测 TOOL-055 无键级枚举——无需同步；若设计期增断言则同步文档）。

### ⑦ 假设显式化
- **A3-1（已验证）**：CALIBRATION 增键无消费方破坏——grep 实测仅模块内消费（review 结论与本分析独立复核一致）。
- **A3-2（假设）**：披露措辞采用 review 建议原文（英文键值）——是否中文化/措辞调整待设计，不影响验收。

---

## 4. 测试静态版本钉机检面（DEC-213③——M-1 口径盲区规则化）

### ① 来源锚点（已核实）
- **DEC-213③**（decision-log L154）：「M-1 口径盲区规则化（FIX-352/353 教训——**同类第 5 次复现**）：测试/夹具静态版本钉 MUST 从被测面渲染源派生，MUST NOT 钉发布字面量；0.85.0+ 候选=纳入 M-1 检查清单或**新增机检面（全仓 grep "0.8x.0" 测试面静态钉审计）**」。
- **DEC-213⑦**：fixture governance-init.md 4 处版本钉三门禁全盲（version-consistency 13 面/projection-sync 26 面/entry-sync 均不覆盖）→ FIX-354 修复 + **0.85.0+ 机检面候选（与③同族）**。
- **FIX-352/353**（plan-tracker L91-92，EVD-1082/1083）：已动态化两处（test_entry_projection / test_bootstrap_aggregate）——机检面仍缺（教训规则化≠机检化）。
- **同类第 5 次复现的教训清单**（DEC-213③ 原文锚定；FIX-354 为第 5 次——EVD-1084）。

### ② file targets（候选面——**新机检的落点属设计决策，标注「待设计确认」**）
| 文件 | 角色 | 证据 |
|------|------|------|
| `skills/software-project-governance/infra/checks/version.py` | 候选扩展点 A——既有版本一致性检查载体（DEC-213⑦ 点名「version-consistency 13 面」不覆盖测试面钉） | checks/ 目录 Read 实测存在；REQUIRED_SNIPPETS 校验面先例（review-FEAT-038 ⑤：checks/version.py:89 正则） |
| `skills/software-project-governance/infra/registry.py` + `infra/verify_workflow.py` | 若新增独立 Check/leaf 的接线面 | registry L194/L293-294（check-injection-budget 注册先例）；test_registry.py L89-90 冻结面（88→89 keys）+ contract_matrix/snapshots.json regen（review-FEAT-028 F-2 先例——新增段必触发 5 个未声明冻结面） |
| `skills/software-project-governance/infra/tests/test_registry.py` / `contract_matrix/snapshots.json` / `core/architecture-baseline.json` | 冻结面联动（若新增 Check） | review-FEAT-028-DESIGN-R0 F-2 实证的新增面清单；archguard baseline `anchor_loc` |
| 审计对象面（只读） | 全仓测试/夹具文件的版本字面量——扫描试点先例：FIX-354 期「git grep 0.83.0 全豁免清单（历史文档/对照引用/测试合成数据——零活跃版本声明面）」（EVD-1084） | EVD-1084 原文 |

**待设计确认**：①扩展既有 check（version-consistency）vs 新增独立 Check/leaf；②扫描形态（字面量 grep vs AST）；③豁免清单机制（FIX-354 期人工豁免清单先例 vs 声明式豁免表）。本分析不裁决。

### ③ 依赖关系
- 与候选 5 同族（fixture 同步契约/版本钉盲区），可同批设计；技术上互不阻塞。
- 对既有任务的依赖：无（FIX-352/353/354 已闭环；本项是其防复发面）。
- 与 RISK-046 同族关注（triage files 声明纪律）——若新机检登记为新任务，change-triage 的 files 面应含派发预授权扩展面（DEC-206⑥/DEC-207③ 口径）。

### ④ 优先级建议：**P2**
理由：DEC-213③ 明文「同类第 5 次复现」——复发频率高、每次都是发布阻塞级代价（FIX-352/353 为 REL-080 阻断 #1/#2）；但非当前活跃缺陷（已动态化两处 + FIX-354 修复第 5 处），属防复发投资。授权序 ④。

### ⑤ 非目标
- 不重做已动态化的 FIX-352/353 修复（验收其形态不改）。
- 不把 M-1 人工检查清单替代为机检（DEC-213③ 给了两个选项——本分析不预先裁决，但机检面一旦落地应覆盖检查清单所辖面）。
- 不扫描非测试面的版本字面量（历史文档/CHANGELOG/对照引用属豁免面——FIX-354 豁免清单先例）。

### ⑥ 验收信号
1. 注入红相：在测试文件临时钉一个过期版本字面量（隔离工作树）→ 机检 FAIL（可翻红）。
2. 对当前树全量扫描 exit 0（零活跃误报；豁免面显式披露——FIX-354 豁免清单口径）。**执行顺序声明（REL-081 双审裁决 D-F-5）：本信号在 ⑦（RISK-056 回放族）合并后执行**——⑦ 合并前执行会与回放族假红/噪音基线互相干扰（若 ④ 走 M-1 清单路径而非新机检面，则无此顺序依赖）。
3. DEC-213⑦ 三门禁盲区面（version-consistency/projection-sync/entry-sync 之外的测试面钉）纳入覆盖披露。
4. check-governance 聚合面可见该机检结果（新 Check 或既有 Check 扩展段）。

### ⑦ 假设显式化
- **A4-1（未验证）**：静态字面量扫描的误报率可控。依据：FIX-354 期全仓 grep 0.83.0 命中可被人工豁免清单归零，但未量化「每版本 bump 后新增命中数」。验证计划：设计期以当前版本字面量试跑全仓扫描并输出命中分类清单。
- **A4-2（未验证）**：新 Check 的冻结面代价（registry/test_registry/contract_matrix/archguard baseline 五面联动——review-FEAT-028 F-2 先例）不超出本候选预算。验证计划：设计期按 FEAT-028 F-2 清单逐面核对。

---

## 5. governance-status.md fixture 同族缺口（FIX-354 R1 发现——byte_copy promote 或 legacy 声明二选一）

### ① 来源锚点（已核实）
- **EVD-1084**（evidence-log L2278，FIX-354 交付行）：「**同族发现**（R1 登记）：governance-status.md fixture 同型缺口（**pre-FIX-270 陈旧无版本标记**）→ **0.85.0 裁决候选**」——任务池所称「FIX-354 R1 发现」的原始落点。
- **FIX-354 先例**（REVIEW-REL-080-RELEASE-R1 F-05）：governance-init.md fixture 同型缺口的修复=byte_copy promote（`fixture-command-governance-init`）+ release-projection --write；「拒绝手工 bump 的裁决：手工改会使 FIX-238.2 陈旧标记失真（文件内容非模板渲染产物即无同步路径）」。
- **二选一机制面**（version-projections.json L199-273 Read 实测）：`declared_legacy_snapshots` 的机器守卫语义——「`check_legacy_snapshots` reddens if the file disappears, and reddens if it ever converges (**a converged copy must be promoted to a real projection** instead of keeping a stale 'legacy' label)」（L208 fixture-engine reason 原文）——即「byte_copy promote 或 legacy 声明」二选一是注册表内建的两种合法终态。
- **DEC-215②**：候选序 ⑤。

### ② file targets（Read/Grep 实证）
| 文件 | 角色 | 证据 |
|------|------|------|
| `project/e2e-test-project/commands/governance-status.md` | **缺口本体**——fixture 副本 | Grep 实测文件存在且被消费（`infra/tests/test_verify_workflow.py` L3031/L3068 读作 fixture_status；EVD-520/EVD-387 历史同步先例） |
| `skills/software-project-governance/core/version-projections.json` | **裁决落点**——当前**既不在** `projections`（byte_copy 合同只覆盖 governance-init/governance 路由/scenario-a~f/bootstrap/snapshot-schema/overview 共 12 条 fixture-command-*，Read L132-197 逐一核对**无 governance-status 条目**）**也不在** `declared_legacy_snapshots`（scope 仅 `infra/`，L199-272） | Read 全文实测——缺口实态成立 |
| `skills/software-project-governance/core/manifest.json` | promote 时原子同步面（`release_projection_contract.projection_ids` +1——FIX-354 先例） | manifest L38 byte_copy kind 在契约集内 |
| `commands/governance-status.md`（canonical） | promote 时的 byte_copy source | Grep 实测存在（166 行级；review-FEAT-036 载体面） |
| e2e 消费面 | promote/declared 后的回归面 | `test_verify_workflow.py` L3016-3068（GovernanceStatusContract 族读 fixture 对比）；e2e-check target_fixture 检查 |

**若走 legacy 声明**：新增 `declared_legacy_snapshots` 条目（id/path/canonical/scope/reason 五键，按 L203-272 条目形态）+ `check_legacy_snapshots` 守护自动生效（红于消失/收敛）。

### ③ 依赖关系
- 与候选 4 同族（fixture 版本钉盲区），设计期可统一审视；执行上互不阻塞。
- 对既有记录的依赖：EVD-1084 登记的裁决义务（0.85.0 裁决候选）；FIX-354 的先例约束（不得手工 bump）。
- 若 promote：依赖 `release-projection --write` 重生成流程（FIX-253 起的既定纪律）。

### ④ 优先级建议：**P2**
理由：缺口已实证（EVD-1084 登记+本分析 Read 复核注册表双缺席）；风险形态=governance-status.md canonical 演进（如 FEAT-036 双契约改动、未来瘦身）后 fixture 静默陈旧——正是 FIX-354 在 governance-init 上已付过学费的同型问题；但当前无活跃 FAIL（e2e target_fixture 检查通过中）。授权序 ⑤。

### ⑤ 非目标
- 不扩大到其他未登记 fixture 的全量普查（本候选只裁决 governance-status.md 一个条目；若 promote/declared 过程中发现新的未登记副本，另立候选——防范围漂移）。
- 不改 e2e fixture 的 `.governance/` 数据内容（fixture-plan 投影已管辖 plan-tracker 版本行）。
- 不动 declared_legacy_snapshots 既有 10 条目（RISK-039/FEAT-038 P2-4 裁决域）。

### ⑥ 验收信号（按裁决方向二选一）
- **promote 方向**：version-projections.json +1 条 byte_copy（source=commands/governance-status.md）+ manifest projection_ids 原子同步；`release-projection --write` 后 fixture 与 canonical SHA256 全同；`check-projection-sync`/`release-projection`（check-only）exit 0；e2e target_fixture 检查全绿。
- **declared 方向**：declared_legacy_snapshots +1 条目（reason 含 pre-FIX-270 陈旧依据 + 不可收敛理由）；`check_legacy_snapshots` 对该条目双向红绿验证（删除文件→红；使其与 canonical 逐字节同→红）。
- 两方向共同：裁决理由入 DEC（沿用 FIX-354/EVD-1084 登记链）。

### ⑦ 假设显式化
- **A5-1（未验证）**：fixture 与 canonical 的分歧量未知（EVD-1084 只记「pre-FIX-270 陈旧无版本标记」；FEAT-036 R1 后 canonical 有双契约改动，fixture 是否同步过未考）。验证计划：裁决前先 diff 实测（字节数+语义差异清单），差异量直接影响二选一裁决——**本分析建议把 diff 实测列为设计确认前置**。
- **A5-2（未验证）**：e2e 消费面（L3031/L3068 等）是否依赖 fixture 的陈旧内容（若依赖，promote 后测试需同步改）。验证计划：promote 试运行（--write 后跑 GovernanceStatusContract 族 + e2e-check）。
- **A5-3（假设）**：本候选裁决权属 Coordinator/用户（DEC-215② 用词「裁决候选」）——本分析只提供两方向的实施面与验收信号，不预设方向。

---

## 6. P3 族：FEAT-038 P3-1/2/5、FEAT-039 F-1/2/5+P3-3/4/7、FEAT-040 P3-1~4+P3-R1-1~4、PLAN_TRACKER_UPGRADED 死夹具清理、FIRST_RUN_DEMO 19→20

### ① 来源锚点（已核实，逐子项）
| 子项 | 锚点 | 原文定位 |
|------|------|---------|
| FEAT-038 P3-1 | REVIEW-FEAT-038-CODE-R0 L144 | 标题重复与层级跳变——六份 scenario-*.md L1/L6 wrapper H1+搬移 H2 同题；overview.md L70/L74、L135/L140 同题对 + L40 孤立 H3 |
| FEAT-038 P3-2 | 同上 L145 | 披露过时——申报「test_registry 88-key 未补」与当前树不符（88 键已在场）；失败清单口径需按当前树刷新 |
| FEAT-038 P3-5 | 同上 L148 | commands/governance.md L86-91 六摘要表触发条件列 D/E/F 行措辞不齐（应统一 `scenario_hint == "X"` 格式） |
| FEAT-039 F-1 | REVIEW-FEAT-039-CODE-R1 L222 + DEC-211④ | ImportError 分支加 WARN（persona 解析器先例） |
| FEAT-039 F-2 | 同上 L181/L195 + DEC-211④ | extract_marked_block 死码降格 LEGACY（旧边界助手 + 2 个旧测试钉住不被使用的实现） |
| FEAT-039 F-5 | 同上 L222 + DEC-211④ | 入口源解析缓存优化 |
| FEAT-039 P3-3 | 同上 L86 + DEC-211③ | `injection_budget_tier_gate()` hard 默认无触发路径——0.85.0 翻 hard 时补断言（→ 候选 1 依赖） |
| FEAT-039 P3-4 | 同上 L87 + DEC-211③ | `--format json` 丢弃 issues 字段（cmd_check_injection_budget L743-744 过滤 `k != "issues"`）+ TOOLS.md L585 说明缺 |
| FEAT-039 P3-7 | 同上 L90/L146/L242 + DEC-211③ | triage files 面 11 文件 vs 机录 1（`.governance/change-triage/FEAT-039.json:10-12` 仍仅 verify_workflow.py）——影响 check_conflicts 重叠检测输入面 |
| FEAT-040 P3-1 | REVIEW-FEAT-040-CODE-R0 L27/L130 | revert_contract_issues 规则④（重复 invariant id）无负例 |
| FEAT-040 P3-2 | 同上 L37/L135 | env 原值回显进 `behavior.env_value` 与 `invalid[].value`（L253-255/L235-238） |
| FEAT-040 P3-3 | 同上 L45/L140 | next_action_line（L299-301）5 不变量散文与 SAFETY_INVARIANTS（L120-151）双源 |
| FEAT-040 P3-4 | 同上 L53/L145 | 预算 clamp 无 behavior 专属档（bootstrap_aggregate.py L769-829）+ canonical-bootstrap-version count=4 命名张力（存量） |
| FEAT-040 P3-R1-1 | REVIEW-FEAT-040-CODE-R1 L270-276 + DEC-212⑤ | 两提示占满配额丢弃顺序未声明（最坏挤掉候选行——DEC-143 投影）+ CliSmokeTests 活体耦合（L518-545） |
| FEAT-040 P3-R1-2 | 同上 L277-283 + DEC-212⑤ | census projected 由差值反推不能独立证伪 + 「two independent readers」实为同构拷贝 |
| FEAT-040 P3-R1-3 | 同上 L284-290 + DEC-212⑤ | 两裁剪器口径 65 vs 81（behavior_profile.py `_clip_value` vs bootstrap_aggregate.py `_clip`）+ env_value L280 未裁剪 |
| FEAT-040 P3-R1-4 | 同上 L291-297 + DEC-212⑤ | 申报测试计数（59/32）无法静态复算——Coordinator 以原样 pytest 输出入证据（记录面） |
| PLAN_TRACKER_UPGRADED 死夹具 | **EVD-1083 边缘披露原文**：「PLAN_TRACKER_UPGRADED 全仓无消费方（死夹具——保留+守卫，清理留 0.85.0）」 | Read 实测：`test_bootstrap_aggregate.py` L126-137——L131-133 动态构造、L134-137 静默 no-op→RuntimeError 守卫；全仓 grep 消费方=0（仅定义处） |
| FIRST_RUN_DEMO 19→20 | DEC-215② 候选池文字 + FEAT-036 契约事实链（REVIEW-FEAT-036-CODE-R0/R1：snapshot 契约 20 键、demo 断言 19 字段、缺者=「Flow-unit lanes」） | `verify_workflow.py` L9228 `FIRST_RUN_DEMO_REQUIRED_FIELDS`（19 项，Read/grep 实测）；20 键契约=EVD-1077 |

### ② file targets（Grep/Read 实证，逐子项）
| 子项 | file targets | 状态 |
|------|-------------|------|
| FEAT-038 P3-1 | `commands/governance/scenario-{a,b,c,d,e,f}.md`（L1/L6 区）+ `commands/governance/overview.md`（L40/L70/L74/L135/L140）+ 守护用例 `test_verify_workflow.py` L20534（wrapper H1 强制——改法须保 H1） | 路径实测存在；行号系 review R0 时点，设计期重锚 |
| FEAT-038 P3-2 | 治理记录/披露文本面（evidence/packet 口径刷新）——**非产品代码** | 记录面 |
| FEAT-038 P3-5 | `commands/governance.md` L86-91 | 实测存在（review 行号） |
| FEAT-039 F-1 | `infra/checks/injection_budget.py`（ImportError 分支——具体行待设计确认；persona 解析器先例=review-FIX-316 系 `_read_text` 形态） | 待设计确认（行级） |
| FEAT-039 F-2 | 同文件 `extract_marked_block` 死码 + 2 个旧测试钉（review R1 L181/L195） | 存在（review 实证）；行号设计期重锚 |
| FEAT-039 F-5 | 同文件 `load_injection_surface`（入口源解析） | 存在（TOOLS.md L582 函数清单） |
| FEAT-039 P3-3 | `test_verify_workflow.py` Feat039InjectionBudgetTests（补 `injection_budget_tier_gate("unknown")=="hard"`） | 实测 L20367 区断言面 |
| FEAT-039 P3-4 | `injection_budget.py` cmd_check_injection_budget L743-744 + `infra/TOOLS.md` L585 | Read/grep 实测 |
| FEAT-039 P3-7 | `.governance/change-triage/FEAT-039.json` L10-12——**治理记录面（Coordinator 快速通道直改，非产品代码）** | review R1 L90 实测 |
| FEAT-040 P3-1 | `infra/behavior_profile.py` revert_contract_issues（L342-389）+ `test_behavior_profile.py` 负例 | review R0 行号；文件实测存在 |
| FEAT-040 P3-2 | 同文件 L253-255/L235-238（裁剪或披露声明） | 同上 |
| FEAT-040 P3-3 | 同文件 L299-301 vs L120-151（散文改由 SAFETY_INVARIANTS id 列表拼装——review 建议原文） | 同上 |
| FEAT-040 P3-4 | `infra/bootstrap_aggregate.py` L769-829（behavior 专属裁剪档——若做）+ `core/version-projections.json` L124-131 count=4 命名张力（存量记录） | review R0 行号 |
| FEAT-040 P3-R1-1 | `bootstrap_aggregate.py` `_next_actions`（丢弃顺序注释/实现）+ `test_behavior_profile.py` L518-545 活体耦合隔离 | review R1 行号 |
| FEAT-040 P3-R1-2 | `test_projection_legacy_snapshots.py` L354-365（_legacy_census）+ L190-196（恒等式）+ `infra/release/projection.py`（独立读者） | review R1 行号；文件实测存在 |
| FEAT-040 P3-R1-3 | `behavior_profile.py` L187-192 `_clip_value` + `bootstrap_aggregate.py` L147-149 `_clip`（口径统一或 docstring 披露）+ L280 env_value | review R1 行号 |
| FEAT-040 P3-R1-4 | 记录面——pytest 原样输出入 evidence（Coordinator） | 非产品代码 |
| PLAN_TRACKER_UPGRADED | `test_bootstrap_aggregate.py` L126-137（清理=删除定义+守卫，或补消费方——二选一待设计确认；EVD-1083 倾向清理） | Read 实测 |
| FIRST_RUN_DEMO 19→20 | `verify_workflow.py` L9228-9248（FIRST_RUN_DEMO_REQUIRED_FIELDS 补第 20 项）+ 文档同步面 `commands/governance.md` L514-517 与 `commands/governance/scenario-f.md` L61/L78（「断言其中 19 字段」表述——grep 实测 3 处） | Read/grep 实测 |

### ③ 依赖关系与批次归属（REL-081 双审裁决方案 A 同步——D-F-2）
- **批次归属标注**：**FEAT-039 F-1/F-2/F-5/P3-3/P3-4 五子项 → 批 2**（与候选 1/8 同落「预算面族」串行票；其中 **P3-3 并入批 2.3 翻 hard 同 commit**——DEC-211③「翻 hard 时补」时点绑定）；**⑥ 其余子项留批 1**。
- **FEAT-039 P3-3 → 候选 1**：hard 触发路径断言在翻 hard 同 commit 补（DEC-211③ 原文绑定；方案 A 消除了「批 1 补断言违反时点绑定」的跨批拆散缺陷）。
- **FEAT-039 F-1/F-2/F-5/P3-4 与候选 1/8 同文件**（injection_budget.py）——批 2 内按文件锁串行；原「两形态均可，由 Coordinator 裁决」的开放态已被方案 A 裁决取代（review-FEAT-039-CODE-R1 L222 的合并建议以方案 A 形态落地）。
- **FEAT-040 P3-R1-1/-3 与 FEAT-040 P3-3 同文件**（behavior_profile.py / bootstrap_aggregate.py）——族内可合批（批 1）。
- **FEAT-039 P3-7 / FEAT-040 P3-R1-4 / FEAT-038 P3-2 为治理记录面**——走 M1.2 快速通道，不走产品代码 change-triage。
- 其余子项互相独立。

### ④ 优先级建议：**P3**（族整体）
理由：全部子项为源判 P3 或非阻塞建议；无当前阻断/缺陷面。授权序 ⑥（P3 族置于第 6 位与源判级一致）。族内两个例外标注：FEAT-039 P3-7（triage files 补记）成本极低且影响 check_conflicts 输入面——**建议随 0.85.0 首个产品代码任务的 triage 同批直改**（治理记录快速通道，不需独立排期）；FEAT-039 P3-3 并入批 2.3 翻 hard 同 commit（方案 A——DEC-211③ 时点绑定兑现），F-1/F-2/F-5/P3-4 随批 2 预算面族串行。

### ⑤ 非目标
- 不重开 FEAT-038/039/040 的已闭环裁决（P2-4 声明化裁决、DEC-211/212 处置均维持）。
- FEAT-040 P3-4 的 `canonical-bootstrap-version count=4` 为存量命名张力——只记录不动（review R0 定性 pre-existing）。
- 不把 P3 族打包成单个大任务（违反 D4 一事一 commit；建议按文件聚族分票：injection_budget 族（**批 2**——方案 A）/ behavior_profile+bootstrap_aggregate 族（批 1）/ docs 措辞族（批 1）/ 测试夹具族（批 1））。

### ⑥ 验收信号（族级）
1. 每子项关闭时其源报告 finding 编号可回指（review-FEAT-03x-CODE-Rx P3-x / F-x → commit + EVD）。
2. docs 措辞族改后：`check-projection-sync`/`check-governance-pack-status`/守护测试（L20534 H1 强制）全绿。
3. injection_budget 族改后：58 用例 + Feat039InjectionBudgetTests 全绿；`check-injection-budget --format json` 含 issues 字段（P3-4 验收）。
4. behavior_profile/bootstrap_aggregate 族改后：两文件 pytest 全绿 + 非干扰双臂契约仍逐面相等（FEAT-040 安全边界机检不回退）。
5. 死夹具清理后：`pytest test_bootstrap_aggregate.py` 全绿且 PLAN_TRACKER_UPGRADED 符号全仓 0 残留（grep 验证）；或补消费方后有真实断言消费（二选一的对应验收）。
6. FIRST_RUN_DEMO 19→20 后：`first-run-demo --assert-snapshot` Checked fields=20；文档面 3 处「19 字段」表述同步。

### ⑦ 假设显式化
- **A6-1（假设，待设计确认）**：FIRST_RUN_DEMO 19→20 的意图=把第 20 个 CLI snapshot 字段（Flow-unit lanes）纳入 demo 断言集。依据：FEAT-036 契约事实链（20 键 CLI snapshot、demo 断 19、缺者唯一=Flow-unit lanes）。存在另一解读（维持 19+披露理由），本分析以补齐为建议解读，**最终意图由 Coordinator/设计确认**。
- **A6-2（假设）**：死夹具处置=EVD-1083 倾向的「清理」；「补消费方」是合法备选（守卫本身有价值）。二选一待设计确认。
- **A6-3（未验证）**：FEAT-039 F-1 的 ImportError 分支行级位置未经本分析 Read 定位（review 只给语义）。验证计划：开工前 Read 定位并重锚。
- **A6-4（假设）**：P3 族按文件聚族分票不改变源判级别与验收信号——分票方式是编排建议，非范围变更。

---

## 7. RISK-056 回放族（既有测试失败 30 项——回放 fixture 重锚或版本世界参数化）

### ① 来源锚点（已核实）
- **RISK-056**（risk-log L52）：「既有测试失败 30 项（test_hooks 回放 6 + test_pre_commit_review_evidence 24 + HotFactSource 16 + ExternalProjectValidation 1——**口径随采样面变化**）：治理活数据增长后 hook/evidence 匹配器与内联 fixture（0.82.0 版本世界）漂移——FEAT-035/036 全量验证时发现，worktree 干净 HEAD 复跑原样复现（非新引入）」；缓解：「修复候选=**回放 fixture 重锚或版本世界参数化**（0.85.0+ 评估，随 D6 hooks 张力一并）；发布门禁不因此阻断（非产品面）」。
- **REVIEW-REL-080-RELEASE-R1 V-11 实测**：全量 **30 failed / 3451 passed / 1 skipped**——30 = 既有环境族（`ReviewEvidenceRegexTests` hook 子测 ×24〔根因=本机无 WSL：`WSL_E_DEFAULT_DISTRO_NOT_FOUND`〕+ `PlanTrackerMatcherTests::test_replay_real_plan_tracker_hits` ×6〔活体耦合〕）；R2/R3 复跑同值（0 新增失败基线）。
- **口径注记（如实披露）**：RISK-056 枚举含 HotFactSource 16 + ExternalProjectValidation 1（合计口径 47）；R1 期实测 30F 的采样面=hook 子测 24+回放 6。两口径并存是风险行自载事实（「口径随采样面变化」），本分析不调和，作为设计期逐项归因的输入。

### ② file targets（Glob/Grep 实证）
| 文件 | 位置 | 内容 |
|------|------|------|
| `skills/software-project-governance/infra/tests/test_hooks.py` L202 | `PlanTrackerMatcherTests`（含 `test_replay_real_plan_tracker_hits`——回放 ×6） | Grep 实测类名与行号 |
| `skills/software-project-governance/infra/tests/test_pre_commit_review_evidence.py` L122 | `ReviewEvidenceRegexTests`（hook 子测 ×24——WSL 环境族） | 同上 |
| `skills/software-project-governance/infra/tests/test_verify_workflow.py` L1592 / L10647 | `HotFactSourceConsistencyTests`（×16 口径）/ `ExternalProjectValidationHarnessTests`（×1 口径） | Grep 实测 |
| 另测：`infra/tests/test_fix270_product_gates.py` L126 | `HotFactSourceHostScopeTests`（HotFactSource 家族另一载体——归属哪一口径待设计期归因） | Grep 实测 |
| 回放 fixture 数据面 | 「内联 fixture（0.82.0 版本世界）」——test_hooks/test_pre_commit_review_evidence 内联治理快照数据 | RISK-056 描述；具体内联块设计期定位 |

### ③ 依赖关系
- 独立于其余候选（纯测试面）；与「D6 hooks 张力」的关联（RISK-056 缓解原文「随 D6 hooks 张力一并」）——hooks_drift 4 项待用户一次性重装（session-snapshot 优先级 3），两事相关但不互阻。
- 对既有基线的依赖：R1/R2/R3 建立的「30F/0 新增」发布基线——修复后该基线数字将变化，M-1 口径（DEC-213③ 教训）要求基线更新如实披露。

### ④ 优先级建议：**P3**（建议，附升 P2 条件）
理由：RISK-056 自评中等严重度、打开中；但影响面=测试红噪音（掩盖真实回归信号）非产品缺陷，发布门禁不阻断（风险行明示）；DEC-215② 授权序置 ⑦。**升 P2 条件**：若 0.85.0 批次内出现需要全量绿基线的门禁收紧动作（如候选 4 新机检接入聚合面），回放族假红将直接干扰新门禁——届时同批提前。（REL-081 双审裁决后：④ 新机检路径的全量扫描验收已显式声明在 ⑦ 合并后执行——§4⑥ 验收信号 2 顺序声明；Release 批 1「⑦ 最优先」排序与本条同向。）

### ⑤ 非目标
- 不修 WSL 环境依赖本身（24 项 hook 子测的根因是本机无 WSL 发行版——环境族；修复方向是测试对环境的显式声明/跳过语义，不是装 WSL）。
- 不动 hook 产品逻辑（匹配器行为）——除非归因证实匹配器真缺陷（当前归因=fixture 漂移）。
- 不追求全量 0 fail（目标=0 **非环境** 假红；环境族失败需显式化口径而非消数）。

### ⑥ 验收信号
1. 逐项归因表：30（或当前采样面实数）项每项标注 环境族/活体耦合/真实缺陷 三类之一，入 EVD。
2. 活体耦合项（回放 6）：重锚或参数化后，在干净 HEAD + 活体 `.governance` 下稳定 PASS（或显式 skip+理由），连续两次会话复跑结果一致。
3. 环境族项（WSL 24）：显式环境声明后，无 WSL 环境下不再计入 FAIL 口径（skip 或 PASS，非静默）。
4. 全量 pytest 失败数下降且 RISK-056 具备关闭条件证据（按其自载关闭口径）。

### ⑦ 假设显式化
- **A7-1（未验证）**：30 项全部为环境/活体耦合、零真实缺陷——归因来自 FEAT-035/036 期对照与 R1 复跑，但 HotFactSource 16 项未在热记录中逐项归因。验证计划：修复设计期重跑并产出逐项归因表（验收信号 1）。
- **A7-2（假设）**：「重锚 vs 参数化」二选一可在设计期基于内联 fixture 构造决定——本分析不预裁决（RISK-056 缓解原文两个候选方向并列）。
- **A7-3（未验证）**：当前树（0.84.0 发布后）的失败集可能与 30 有出入（语料继续增长）。验证计划：开工首日重跑采样。

---

## 8. entry-skill 独立预算（FEAT-039 注入预算的 entry-skill 独立面）

### ① 来源锚点（已核实）
- **DEC-210(ii)**（decision-log L151）：「真正超限的 entry-skill（8,573 tok）属 report-only 层（skill 加载是功能需要非注入债）」——advisory 出货裁决的三论据之一。
- **DEC-211②**：canonical 口径——「最大单面 entry-skill 8,573 tok（report-only）」。
- **BUDGET_TIER_POLICY 现态**（injection_budget.py L166-179 Read 实测）：`skill` tier = `report-only`（"progressive load — entry skill"），**无独立预算数值**——当前唯一数值预算是 resident 6,000；`INJECTION_BUDGET_SURFACES` L140-147 entry-skill 面（path=`skills/software-project-governance/SKILL.md`，scope=full-file）Read 实测。
- **DEC-215②**：候选序 ⑧（授权序末位）。

### ② file targets（Read/Grep 实证）
| 文件 | 位置 | 内容 |
|------|------|------|
| `skills/software-project-governance/infra/checks/injection_budget.py` L140-147 | entry-skill surface 定义（tier=skill） | Read 实测 |
| 同文件 L171-174 | `BUDGET_TIER_POLICY["skill"]`——独立预算的落点（增 budget 数值/gate 姿态字段） | Read 实测 |
| 同文件 L496-498/L552-604 | `injection_budget_tier_gate`/逐 tier 裁决循环——新预算数值的裁决接线面 | Read 实测（L552 `for name, policy in BUDGET_TIER_POLICY.items()`） |
| `skills/software-project-governance/infra/tests/test_verify_workflow.py`（Feat039InjectionBudgetTests） | 断言面（L20368 现断言 `BUDGET_TIER_POLICY["skill"]["gate"] == "report-only"`——若姿态变更需翻转） | Grep 实测 |
| `skills/software-project-governance/infra/TOOLS.md` L60/L582-592 | TOOL-055 文档面（skill/command 层「单独计量（report-only，不并入 resident）」表述——立独立预算后须同步） | Read/grep 实测 |
| `skills/software-project-governance/SKILL.md` | 被计量本体（8,573 tok；24,769 行锚是引擎行数非 SKILL——注意不要混淆；SKILL 面预算若立 hard 则可能引发瘦身需求，**属另一裁决**） | injection_budget surface path 实测 |

### ③ 依赖关系
- **与候选 1 同文件同表强耦合**（BUDGET_TIER_POLICY/INJECTION_BUDGET_SURFACES + 同一测试类）——**双审裁决（方案 A）：候选 1/8 与 FEAT-039 F-1/F-2/F-5/P3-3/P3-4 全部落批 2 串行**（文件锁协调 MUST；候选 1 先行）。
- 对 FEAT-039 F-1/F-2/F-5/P3-3/P3-4（自候选 6 拆出的批 2 子项）同文件——批 2 内同上串行协调。
- 无对既有任务的其他依赖。

### ④ 优先级建议：**P2**
理由：entry-skill 是全域最大单面（8,573 tok）且零约束（report-only 无预算值）——「预算不守护则回弹」的论理（FEAT-039 立项动机）对 entry-skill 同样成立；但其消费形态=按需加载（非 resident 注入债），DEC-210 已定性「功能需要非注入债」，故不阻断 0.85.0 主线。授权序 ⑧ 与源判一致。

### ⑤ 非目标
- 不把 entry-skill 并入 resident 6,000 预算（DEC-210(ii) 已裁决分层；report-only→hard 的姿态翻转属另一裁决）。
- 不在本候选内做 SKILL.md 瘦身（若独立预算立 hard 且超限，瘦身另立候选——防范围膨胀）。
- 不动 command-doc 面（governance.md 路由层同样 report-only——除非设计确认一并立预算，当前证据不含该诉求）。

### ⑥ 验收信号
1. `check-injection-budget --format json` 的 tiers/skill 面携带独立预算数值字段（现无——验收=字段在且数值可配）。
2. 合成超限（fixture 注入 >预算的 SKILL 面）触发声明的 gate 姿态（advisory 或 hard——按设计裁决），且有翻红测试。
3. TOOLS.md TOOL-055 与 `--format json` 输出一致（无第二口径）。
4. 既有 58+ 用例与 Feat039InjectionBudgetTests 全绿（含翻转后的断言）。

### ⑦ 假设显式化
- **A8-1（假设，待设计确认）**：「独立预算」的意图=为 skill tier 设独立 budget 数值 + gate 姿态字段（现 policy 行无数值）。依据：DEC-210(ii) 以 8,573 tok 数值论证但该数值无预算承载。备选解读：仅为 entry-skill 单面（非全 skill tier）立预算——两形态 file targets 相同，验收信号 1 的字段粒度不同。
- **A8-2（未验证）**：独立预算数值档位（8,573 现值 + 余量 vs 收紧目标）——属关键决策，须 Coordinator/用户裁决，本分析只交付实施面。
- **A8-3（假设）**：与候选 1 同批不产生裁决耦合（resident 翻 hard 与 skill 立预算互相独立）——但共享 policy 表结构改动，实现顺序需设计确认。

---

## 硬门槛自检（Analyst 任务书四条）

| 门槛 | 判定 | 证据 |
|------|------|------|
| 每项分析可追溯到治理记录锚点 | **PASS** | 8/8 项均有 DEC/RISK/EVD/review 编号 + 文件行级定位（§1~§8 ①）；全部编号经本会话 Grep/Read 核实，无凭池面文字臆断 |
| file targets 全部实证存在 | **PASS**（2 处设计确认面如实标注） | 产品文件路径/行号均来自本会话 Grep/Read；候选 4 的新机检落点与候选 6 内 F-1 行级位置属「待设计确认」——已显式标注并列出候选面，未臆断为既成事实 |
| 假设显式化 = 100% | **PASS** | A1-1~A8-3 共 **23** 条假设（3+3+2+2+3+4+3+3——REL-081 D-F-6 计数勘误：初版自检误报 21），每条含未验证/已验证定性 + 验证计划（或已验证证据） |
| 不做技术选型/不写代码/不改 .governance/ | **PASS** | 本文件为唯一写入面（docs/planning/）；文中所有「修法」均转述源报告建议并列二选一/待设计确认，无选型裁决 |

**requirement-clarification 框架对照（每候选）**：问题陈述（来源锚点=登记残留事实）/ IN-OUT（非目标节，每项 ≥1 条 OUT+理由）/ 假设与约束（⑦）/ 验收信号可验证（⑥）——8/8 项结构齐备。

---

## 10. 边缘问题（提交 Coordinator 的裁决输入）

1. **候选间文件冲突（check_conflicts 输入面）——已按 REL-081 双审裁决方案 A 收口（D-F-2 同步）**：同触 `infra/checks/injection_budget.py` + 同一测试类的最终集合 = **候选 1 + 候选 8 + FEAT-039 F-1/F-2/F-5/P3-3/P3-4（自 ⑥ 拆出），全部落批 2 串行**（P3-3 并入批 2.3 翻 hard 同 commit——DEC-211③ 时点绑定）；⑥ 其余子项留批 1。原「单批串行」建议已被方案 A 取代（两半面编排矛盾消解）；triage files 声明义务不变（FEAT-039 P3-7 教训：漏声明=冲突检测漏面）。
2. **review-FIX-355 其余 P2 未入池**：C-03（UnicodeDecodeError 契约，建议 1 行+1 用例）/C-05（3 分支用例）与候选 2 同文件同报告；C-04（DEC-214 补记）为治理记录面。DEC-215② 只点了 C-01+13 行——若候选 2 开工，同窗顺带 vs 严格分票（D4）由 Coordinator 裁决；本分析默认严格分票、仅登记。
3. **「死夹具」定性溯源**：任务池文字「PLAN_TRACKER_UPGRADED 死夹具清理」的唯一治理锚点=EVD-1083 边缘披露（全仓无消费方）；仓内 grep「死夹具/dead fixture」零命中（DEC-215② 沿用该词）。本候选前提成立，但处置二选一（清理 vs 补消费方）待设计确认（A6-2）。
4. **RISK-056 双口径**：30（R1 实测采样面）vs 47（风险行枚举 6+24+16+1）并存——风险行自载「口径随采样面变化」；设计期逐项归因前不调和（A7-1）。
5. **候选 5 方向未决**：promote vs declared 的裁决属 Coordinator/用户（DEC-215②「裁决候选」用词）；本分析实证了「双缺席」缺口实态（既不在 projections 也不在 declared_legacy_snapshots），两方向实施面与验收信号均已备好（§5⑥），A5-1 的 diff 实测建议列为裁决前置。
6. **候选 1 瘦身可行性是全批次最大不确定项**：A1-1（3,953/4,154 tok 可压缩余量是否存在）未验证——已编排批 2 前置「2.0 量测」步骤（REL-081 F-1 修复）作为开工否决点；量测否决/瘦身后不达标/翻 hard 前不可达时按 Release 文档修订后的**单一回退决策树**三节点处置（本分析不再独立枚举回退选项——「阈值复议 vs 二段瘦身」独立枚举已删除，防三处不一致复发）。**授权依据勘误（Design R0 F-4 核对）：「阈值复议」的依据 = DEC-211②「DEC-210 论证不依赖具体数字档位」——初版所写「DEC-210 预留选项」属引申表述，DEC-210 原文无预留选项。**
7. **行号时效**：全文行号基于 HEAD `d62066b`（2026-09-19）；候选 2 的 review-FIX-355 复审基线条款（blob `e681bfa`/`610c2ef` 变更即证据面失效须重跑 V-1~V-16）应在候选 2 任务书原样继承。

---

*分析方：Analyst Agent（只读分析 + 本文件单点写入）｜任务：0.85.0-PLAN/A｜依据：agents/analyst.md + skills/requirement-clarification/SKILL.md｜上游授权：DEC-215②*
